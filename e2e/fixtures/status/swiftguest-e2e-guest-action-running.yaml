# Status of the M6 Stop subject (SPEC-0010). No KubeSwift controller runs in the
# E2E cluster, so what one would write is injected here.
#
# The three facts the Stop dialog reads: the phase it starts from, the address a
# `nat`-bound guest releases with its pod (B7), and the launcher pod the
# click-time read verifies before offering to delete it (B3), which
# 55-launcher-pods.yaml really creates.
status:
  phase: Running
  nodeName: __NODE_NAME__
  restartCount: 0
  network:
    primaryIP: 10.244.1.31
    ready: true
    interface: eth0
  runtime:
    hypervisor: cloud-hypervisor
    pid: 4311
  podRef:
    apiVersion: v1
    kind: Pod
    name: e2e-guest-action-running-launcher
    namespace: kubeswift-e2e
  conditions:
    - type: Ready
      status: "True"
      reason: GuestRunning
      message: The guest is running.
      lastTransitionTime: "2026-08-28T09:00:00Z"
      observedGeneration: 1
